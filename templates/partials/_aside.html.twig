<aside class="relative bg-sidebar h-screen">
    <div class="logo">
        <a href="{{ path('app_home') }}"><\></a>
    </div>
    <nav>
        {% for activeYear in list_active_years() %}
            <a href="{{ path('app_year_days', { 'year': activeYear.title }) }}" class="flex nav-item {% if year is defined and year.title == activeYear.title %} active {% endif %}">{{ activeYear.title}}</a>
        {% endfor %}
    </nav>
</aside>