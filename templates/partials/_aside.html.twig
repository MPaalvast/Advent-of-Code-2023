{% import "partials/_aside_item.html.twig" as menuItems %}

<aside class="relative bg-sidebar h-screen w-64 hidden sm:block shadow-xl overflow-y-auto">
    <div class="p-6">
        <a href="{{ path('app_home') }}" class="text-white text-3xl font-semibold uppercase hover:text-gray-300"><\></a>
    </div>
    <nav class="text-white text-base font-semibold pt-3 overflow-y-auto">
        {% include 'partials/_aside_item_list.html.twig' %}
    </nav>
</aside>