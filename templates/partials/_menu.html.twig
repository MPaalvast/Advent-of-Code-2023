<!-- Desktop Header -->
<header class="w-full items-center bg-white py-2 px-6 hidden sm:flex">
    <div class="w-1/2"></div>
    <div x-data="{ isOpen: false }" class="relative w-1/2 flex justify-end">
        Advent of code |
        <a href="{{ path('admin') }}">
            <span class="menu-item-label position-relative">
                Admin
            </span>
        </a>
    </div>

</header>

<!-- Mobile Header & Nav -->
<header x-data="{ isOpen: false }" class="w-full bg-sidebar py-5 px-6 sm:hidden overflow-y-auto">
    <div class="flex items-center justify-between">
        <a href="{{ path('app_home') }}" class="text-white text-3xl font-semibold uppercase hover:text-gray-300"><\></a>
        <button @click="isOpen = !isOpen" class="text-white text-3xl focus:outline-none">
            <i x-show="!isOpen" class="fas fa-bars"></i>
            <i x-show="isOpen" class="fas fa-times"></i>
        </button>
    </div>

    <!-- Dropdown Nav -->
    <nav :class="isOpen ? 'flex': 'hidden'" class="flex flex-col pt-4 overflow-y-auto">
        <a href="{{ path('app_year_days', {'year': 2023}) }}" class="flex items-center text-white py-4 pl-6 nav-item">2023</a>
        <a href="{{ path('app_year_days', {'year': 2024}) }}" class="flex items-center text-white py-4 pl-6 nav-item">2024</a>
{#        {% include 'partials/_aside_item_list.html.twig' %}#}
    </nav>
</header>